### Пример 1

#### Было

```java

public class PlatesRegimeKeeper {
    private boolean isPlatesEnabled = false;

    public boolean isPlatesEnabled() {
        return isPlatesEnabled;
    }

    public void setPlatesEnabled(boolean platesEnabled) {
        isPlatesEnabled = platesEnabled;
    }
}

public class Tag {
    private String id;
    private String value;

    public Tag(String id, String value) {
        this.id = id;
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    public void setValue(String value) {
        this.value = value;
    }
}


public class GroupListAdapter {
    public void setAllComments(List<Comment> allComments) {
        this.allComments = allComments;
        groupNamesSet = new TreeSet<>();
        if (PlatesRegimeKeeper.isPlatesEnabled()) groupNamesSet.add("Плиты");
        else for (Comment com : allComments) {
            List<Tag> taskTags = com.getTask().getTags();
            for (Tag tag : taskTags) {
                if (tag.getValue().substring(0, 1).equals(FiltersNames.GROUPWORK.substring(0, 1))) {
                    groupNamesSet.add(tag.getValue().split("/")[1]);
                }
            }
        }
    }
}
```

#### Стало

```java

```