### Пример 2

#### Было

```java
public class GroupTileView {

    private List<Comment> commentsOfGroup;
    
    private String getTasksCountInfo() {
        if (commentsOfGroup.size() > 4)
            return (commentsOfGroup.size() + " задач");
        else if (commentsOfGroup.size() > 1)
            return (commentsOfGroup.size() + " задачи");
        else if (commentsOfGroup.size() == 1) return ("1 задача");
        else return ("Задачи отсутствуют");
    }
}
```

#### Стало